# CU_Chatbot
Azure based ChatGPT backed app,


```bash
docker compose up --build
```

```bash
cp .env_sample .env
```

```bash
source .env
$ curl http://$HOST:$PORT
{"message":"Hello, FastAPI in Docker!"}
```