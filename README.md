# CU_Chatbot
Azure based ChatGPT backed app,

```bash
cp .env_sample .env
```

```bash
docker compose up --build
```

```bash
source .env
$ curl http://$HOST:$PORT
{"message":"Hello, FastAPI in Docker!"}
```